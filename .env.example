# TeamFlow — корневой шаблон окружения для Docker Compose
# Скопируйте этот файл в .env и заполните локальными значениями.
# НИКОГДА не коммитьте .env в git.
#
# Эти хостнеймы резолвятся ВНУТРИ сети Docker Compose:
#   postgres / redis / rabbitmq — имена сервисов Docker (не localhost)

# Приложение
ENVIRONMENT=dev

# PostgreSQL — креды общие для сервиса postgres и приложения
DATABASE_URL=postgresql+asyncpg://teamflow:changeme@postgres:5432/teamflow
DB_POOL_SIZE=10
DB_MAX_OVERFLOW=5
POSTGRES_USER=teamflow
POSTGRES_PASSWORD=changeme
POSTGRES_DB=teamflow

# Redis
REDIS_URL=redis://redis:6379/0

# RabbitMQ — креды общие для сервиса rabbitmq и приложения
RABBITMQ_URL=amqp://teamflow:changeme@rabbitmq:5672/
RABBITMQ_USER=teamflow
RABBITMQ_PASSWORD=changeme

# JWT — секрет для подписи токенов (минимум 32 символа)
JWT_SECRET=changeme-please-use-a-long-random-secret-32
ACCESS_TOKEN_TTL=900
REFRESH_TOKEN_TTL=604800

# Первый суперпользователь — создаётся идемпотентно при старте приложения.
# Пароль: минимум 12 символов, со спецсимволом.
FIRST_SUPERUSER_EMAIL=admin@example.com
FIRST_SUPERUSER_PASSWORD=ChangeMe123!

# Подпись сессионных cookie SQLAdmin (минимум 32 символа)
ADMIN_SESSION_SECRET=changeme-please-use-a-long-random-secret-32
